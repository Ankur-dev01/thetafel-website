/**
 * Onboarding entry placeholder.
 * Replaced in D2 with the service-picker (Step 0).
 * The OnboardingShell in layout.tsx handles auth redirects before this renders.
 */
export default function OnboardingPage() {
  return (
    <div className="flex items-center justify-center min-h-screen text-[#9c8b6a] text-sm">
      Step pages coming in D2.
    </div>
  )
}
